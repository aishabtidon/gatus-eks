# Lab/cost tradeoffs — still scanned via Checkov soft_fail; these are skipped to keep CI green.
skip-check:
  - CKV_AWS_355 # IAM Resource "*" on list/describe-style Route53 actions
  - CKV_AWS_79  # IMDS v1 on launch template (EKS managed nodes)
  - CKV_AWS_39  # EKS public API endpoint enabled (needed for CI/kubectl)
  - CKV_AWS_58  # EKS secrets encryption (extra KMS cost)
  - CKV_AWS_38  # Public endpoint open (lab; tighten with your IP in prod)
  - CKV_AWS_338 # CW log retention < 1 year (we disable control-plane logs)
  - CKV_AWS_158 # CW log group KMS
  - CKV_AWS_51  # ECR tag immutability
  - CKV_AWS_136 # ECR KMS encryption
