grafana:
  adminPassword: "GatusGrafana123!"
  service:
    type: ClusterIP
  persistence:
    enabled: false

prometheus:
  prometheusSpec:
    retention: 3d
    resources:
      requests:
        cpu: 100m
        memory: 512Mi

alertmanager:
  enabled: true
